public class Converter {
    public static double pathCalc(int steps) {
        double path = steps * 0.00075;
        return path;
    }
    public static double calorCalc(int steps) {
        double kiloCalor = steps * 0.05;
        return kiloCalor;
    }
}
